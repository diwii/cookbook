**Timeout**
```javascript
setTimeout(() => {
    isAnimating = false; // Set to false after 1 second
}, 1000);
```

**Interval**
```javascript
// Call function every 100ms
setInterval(() => {
    console.log('tick');
}, 100);
```
