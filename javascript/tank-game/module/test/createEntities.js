import { Entity } from '../entity.js';
import GAME from '../game.js';
import { Particle } from '../particle.js';

export function createEntities() {
    // let entity = new Entity;
    // entity.color = 'blue';
    // entity.width = 100;
    // entity.height = 100;
    // entity.speed = 2;
    // entity.direction.moveX = 1;
    // entity.direction.moveY = 0;

    // let entity2 = new Entity(0, 0, 100, 100);
    // entity2.color = 'green';
    // entity2.speed = 5;
    // entity2.direction.moveX = 0;
    // entity2.direction.moveY = 1;

    // let entity3 = new Entity(0, 0, 100, 100);
    // entity3.color = 'red';
    // entity3.speed = 9;
    // entity3.direction.moveX = 1;
    // entity3.direction.moveY = 1;

    // let entity4 = new Entity(0, 50, 50, 50);
    // entity4.color = 'gold';
    // entity4.speed = .1;
    // entity4.direction.moveX = 1;
    // entity4.direction.x = -1;

    // const collidingEntity = new Entity;
    // collidingEntity.x = GAME.ctx.canvas.width / 2 - 50;
    // collidingEntity.y = GAME.ctx.canvas.height / 2 - 50;
    // collidingEntity.width = 100;
    // collidingEntity.height = 100;
    // collidingEntity.color = 'blue';
}
